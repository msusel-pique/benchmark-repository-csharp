@model Eleflex.Security.Web.Security.Roles.RolesViewModel
@{
    ViewBag.Title = "Manage Role Roles";
}
<h1>Manage Role Roles</h1>
<div class="container">
    <div class="well">
        @Html.ActionLink("Back to Role", "Edit", new { roleKey = Model.RoleKey }, new { @class = "btn btn-sm btn-primary", @type = "button" })
        <span><label>Rolename:</label> @(Model.RoleName)</span>
    </div>
    <h2>Assigned Roles</h2>
    @Html.ActionLink("Add New Role", "EditRole", new { roleKey = Model.RoleKey }, new { @class = "btn btn-primary", @type = "button" })
    <table class="eleflexdatatable">
        <thead>
            <tr>
                <th>Name</th>
                <th>Status</th>
                <th>Start</th>
                <th>End</th>
                <th>Comment</th>
                <th style="width:150px;">Action</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.RoleRoles != null)
            {
                for (int i = 0; i < Model.RoleRoles.Count; i++)
                {
                    <tr>
                        <td>@Model.RoleRoles[i].RoleName</td>
                        <td>@if(Model.RoleRoles[i].Inactive){<text>Inactive</text>}else{<text>Active</text>}</td>
                        <td>
                            @if(@Model.RoleRoles[i].StartDate.HasValue)
                            { @Model.RoleRoles[i].StartDate.Value.ToLocalTime().DateTime}
                        </td>
                        <td>
                            @if (@Model.RoleRoles[i].EndDate.HasValue)
                            { @Model.RoleRoles[i].EndDate.Value.ToLocalTime().DateTime}
                        </td>
                        <td>@Model.RoleRoles[i].Comment</td>
                        <td>
                            @Html.ActionLink("Edit", "EditRole", new { roleKey = Model.RoleKey, roleRoleKey = Model.RoleRoles[i].RoleRoleKey }, new { @class = "btn btn-sm btn-primary", @type = "button" })
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>


</div>
<script type="text/javascript">
    $(document).ready(function () {

    });
</script>
